import { FC } from 'react';
import { Container, Col } from 'react-bootstrap';

const Home: FC = () => {
	return (
		<Container className="d-flex flex-grow-1">
			<Col>Home page</Col>
		</Container>
	);
};

export default Home;
