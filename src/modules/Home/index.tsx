import { Container, Row, Col } from 'react-bootstrap';

interface Props {}

const Home = (props: Props) => {
	return (
		<Container fluid>
			<Row>
				<Col>3 of 1</Col>
			</Row>
		</Container>
	);
};

export default Home;
